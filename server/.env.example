# SitNRide Railway backend - environment variables
#
# Phase 1 needs NONE of these to boot. They are listed here as documentation
# for Phase 2 (payment routes). Do NOT commit a real .env file.

# Injected automatically by Railway - do NOT set manually in production
PORT=3000
NODE_ENV=production

# ---- Phase 2 (NOT used yet) -------------------------------------------------
# STRIPE_SECRET_KEY=sk_test_...
# SUPABASE_URL=https://xxxx.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=eyJ...
